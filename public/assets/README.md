# Theme assets moved

Metronic assets now live under `public/themes/`:

- `public/themes/metronic8/assets/` — Bootstrap 8.x bundles
- `public/themes/metronic9/assets/` — Tailwind 9.x bundles

Configure the active theme via `UI_THEME` in `.env`.
See `public/themes/README.md` for details.
