# Metronic 8 demo files (reference only)

These files were moved out of the live theme partials during cleanup.
They are not included by the app shell. Copy pieces back into `partials/` when you add drawers, widgets, or demo modals in a future phase.
